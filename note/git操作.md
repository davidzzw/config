```
git config --global user.email zhangzewei@chezhibao.com
git commit -m msg
git add
git push origin HEAD:refs/for/master
git reset version 回滚分支
git commit –amend –author=’1315377399@qq.com’
git checkout MID-4 切换分支
git pull origin master 从master分支拉代码
git checkout -b MID-5 origin/MID-5
git merge branch
git remote -v 查看你当前项目的远程git地址
git branch -a :查看全部分支（远程的和本地的）
git branch -l :查看本地分支
git branch -r :查看远程分支
```

